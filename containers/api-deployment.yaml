apiVersion: apps/v1
kind: Deployment
metadata:
  name: nfldata-api
  labels:
    app: nfldata-api
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nfldata-api
  template:
    metadata:
      labels:
        app: nfldata-api
    spec:
      containers:
      - name: nfldata-api
        image: gcr.io/shadowgraph/nfldata-api:local
        resources:
          limits:
            memory: "128Mi"
            cpu: "200m"
        ports:
        - containerPort: 5000
